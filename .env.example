# Port for the server to listen on
PORT=3000

# Secret token required to authenticate API requests
# Choose a strong, random string and use it in your client requests
# e.g., Authorization: Bearer <API_TOKEN>
API_TOKEN=your_secure_token_here
